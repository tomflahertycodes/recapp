<h2><%= @job.role %></h2>
<p><%= @job.location %></p>
<p><%= @job.contract %></p>
<p><%= @job.rate %></p>
<p><%= @job.sector %></p>
<p><%= @job.user.name %></p>

    <% if job.user_id == current_user.id %>
    <p> <%=(link_to 'Edit', edit_job_path(job)) %>
    <%= link_to " Delete",
            job_path(job),
            method: :delete,
            data: { confirm: "Are you sure?" } %></p></li>
  <% end %>
